import React from 'react';
import styles from './Spinner.module.css';

// style wrapper around div html element to show spinner with css
const Spinner = () => <div className={styles.loader} />;

export default Spinner;
